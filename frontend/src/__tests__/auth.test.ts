import { describe, it, expect } from 'vitest'

describe('Auth Utils', () => {
  it('should be tested later as they require mocking localStorage and tokens', () => {
    expect(true).toBe(true)
  })
})
